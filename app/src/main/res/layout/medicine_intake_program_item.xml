<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="5dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_intake_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="Programar toma de medicamento"
        tools:text="Programar toma de medicamento"/>

    <EditText
        android:id="@+id/et_start_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tv_intake_label"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="16:30"/>

    <TextView
        android:id="@+id/tv_start_time_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/et_start_time"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Hora de inicio (24 hrs)"
        android:text="Hora de inicio (24 hrs)"/>

    <EditText
        android:id="@+id/et_start_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tv_start_time_label"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="21/09/2021"/>

    <TextView
        android:id="@+id/tv_start_date_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/et_start_date"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Fecha de inicio"
        android:text="Fecha de inicio"/>

    <Button
        android:id="@+id/btn_program"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        app:layout_constraintTop_toBottomOf="@id/tv_start_date_label"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Program"
        tools:text="Program" />

</androidx.constraintlayout.widget.ConstraintLayout>